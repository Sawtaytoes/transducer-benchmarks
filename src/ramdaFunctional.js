const {
  forEach,
} = require('ramda')

const array = require('./array.js')
const timer = require('./timer.js')

const {
  startProcessing,
  stopProcessing,
} = (
  timer()
)

startProcessing()

forEach(
  (
    Function
    .prototype
  ),
)(
  array,
)

stopProcessing()
