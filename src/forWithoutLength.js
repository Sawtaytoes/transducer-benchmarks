const array = require('./array.js')
const timer = require('./timer.js')

const {
  startProcessing,
  stopProcessing,
} = (
  timer()
)

startProcessing()

for (
  let index = 0,
    length = array.length
  ;index < length
  ;index += 1
) {
  Function
  .prototype()
}

stopProcessing()
