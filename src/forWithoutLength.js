const {
  allowlist,
  array,
  blocklist,
} = require('./arrays.js')
const taskName = require('./taskName.js')
const timer = require('./timer.js')

const tasks = {
  basicLoop: () => {
    for (
      let index = 0
      ;index < array.length
      ;index += 1
    ) {
      Function
      .prototype()
    }
  },
  filterDown: () => {
    for (
      let index = 0
      ;index < array.length
      ;index += 1
    ) {
      Function
      .prototype()
    }
  },
}

const {
  startProcessing,
  stopProcessing,
} = (
  timer()
)

startProcessing()

tasks
[taskName]()

stopProcessing()
