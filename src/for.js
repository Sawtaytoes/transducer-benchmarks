const array = require('./array.js')
const timer = require('./timer.js')

const {
  startProcessing,
  stopProcessing,
} = (
  timer()
)

startProcessing()

for (
  let index = 0
  ;index < array.length
  ;index += 1
) {
  //
}

stopProcessing()
